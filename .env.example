# ============================================
# 小红书爆款内容生成助手 - 环境变量配置
# ============================================
# 复制此文件为 .env 并填入您的配置值
# cp .env.example .env

# --------------------------------------------
# AI API 配置 (必填)
# --------------------------------------------
# 方式一：使用 OpenAI 兼容 API (推荐)
# 支持 OpenAI、Azure OpenAI、Gemini、Claude 等任何兼容 OpenAI API 格式的服务
OPENAI_API_KEY=your-api-key-here
OPENAI_BASE_URL=https://api.openai.com/v1

# 方式二：使用 Manus Forge API (仅限 Manus 平台)
# BUILT_IN_FORGE_API_KEY=your-forge-api-key
# BUILT_IN_FORGE_API_URL=https://forge.manus.im

# 模型配置 (可选)
# 默认使用 gemini-3-flash-preview（Gemini 3.0+），可修改为其他兼容模型
# LLM_MODEL=gemini-3-flash-preview
# LLM_MODEL=gemini-3-pro-preview
# LLM_MODEL=gpt-4.1-mini
# LLM_MODEL=gpt-4.1-nano

# --------------------------------------------
# OAuth 配置 (可选 - 本地开发可跳过)
# --------------------------------------------
# 本地开发模式下会自动使用模拟用户，无需配置
VITE_OAUTH_PORTAL_URL=https://oauth.example.com
VITE_APP_ID=demo-app
OAUTH_SERVER_URL=https://oauth.example.com
JWT_SECRET=your-jwt-secret-for-development

# --------------------------------------------
# 数据库配置 (可选)
# --------------------------------------------
# 如果不配置，历史记录等功能将无法持久化
# DATABASE_URL=mysql://user:password@localhost:3306/xhs_content

# 批量生成并发（可选）
# 建议 2-5。过大容易触发限流/超时。
BATCH_CONCURRENCY=3
